import StateBlock from 'markdown-it/lib/rules_block/state_block';
/**
 * Adds a new _token_ to **markdown-it**'s `StateBlock`
 */
export declare function addToken(state: StateBlock, startLine: number, nextLine: number): boolean;
